version: 4.2.12
session: 67a9c0df39d28eca2b0a44f5
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Assert that the webpage loads successfully.
    commands:
      - command: assert
        expect: the webpage displays "Software Outsourcing Company"
  - prompt: >-
      Assert that the Code4Nord logo is displayed at the top left of the page. 
      # Use the content of the file from the environment
    commands:
      - command: assert
        expect: the webpage displays "code4nord" at the top left
